'use client'; import {Search, Copy, Download, ArrowUpRight} from 'lucide-react'; import {useState} from 'react'; import Link from 'next/link';
const items=[{id:'blox-fruits',name:'Blox Fruits Toolkit',game:'Blox Fruits',version:'2.4.1',author:'VipriX Team',downloads:'1,284',category:'Combat'},{id:'blade-ball',name:'Blade Ball Utility',game:'Blade Ball',version:'1.8.0',author:'Nova',downloads:'896',category:'Utility'},{id:'universal',name:'Universal QoL',game:'Universal',version:'3.1.2',author:'VipriX Team',downloads:'2,104',category:'Quality of life'}];
export default function Scripts(){const [q,setQ]=useState(''); const filtered=items.filter(x=>(x.name+x.game+x.category).toLowerCase().includes(q.toLowerCase())); return <main className="min-h-screen px-5 pb-20 pt-32"><div className="mx-auto max-w-6xl"><Link href="/" className="text-sm text-muted hover:text-foreground">← Back home</Link><div className="mt-8 flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-primary">The library</p><h1 className="mt-3 text-5xl font-black tracking-[-.05em] md:text-7xl">Scripts.</h1><p className="mt-3 text-muted">Browse the latest releases from the community.</p></div><div className="glass flex items-center gap-3 rounded-full px-4 py-3 md:w-80"><Search size={17} className="text-muted"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search scripts..." className="w-full bg-transparent text-sm outline-none placeholder:text-muted"/></div></div><div className="mt-12 grid gap-4 md:grid-cols-3">{filtered.map(s=><article key={s.id} className="glass rounded-3xl p-5 transition hover:-translate-y-1 hover:border-primary/50"><div className="flex items-center justify-between"><span className="rounded-full bg-primary/15 px-3 py-1 text-xs text-violet-200">{s.game}</span><span className="font-mono text-xs text-muted">v{s.version}</span></div><h2 className="mt-8 text-xl font-semibold">{s.name}</h2><p className="mt-2 text-sm text-muted">Maintained by {s.author}. Reliable, documented, and ready for your community.</p><div className="mt-6 flex items-center justify-between border-t border-white/10 pt-4 text-xs text-muted"><span>{s.downloads} downloads</span><span>{s.category}</span></div><div className="mt-5 flex gap-2"><button onClick={()=>navigator.clipboard?.writeText(`-- ${s.name}\n-- Hosted by VipriX Hub`)} className="flex flex-1 items-center justify-center gap-2 rounded-xl bg-primary px-3 py-2.5 text-sm font-semibold transition hover:bg-primary/80"><Copy size={15}/> Copy code</button><a href={`/api/scripts/${s.id}/download`} className="grid size-10 place-items-center rounded-xl border border-white/10 hover:border-primary/50" aria-label="Download"><Download size={16}/></a><Link href={`/script/${s.id}`} className="grid size-10 place-items-center rounded-xl border border-white/10 hover:border-primary/50" aria-label="Details"><ArrowUpRight size={16}/></Link></div></article>)}</div>{!filtered.length&&<p className="py-20 text-center text-muted">No scripts match your search.</p>}</div></main>}
