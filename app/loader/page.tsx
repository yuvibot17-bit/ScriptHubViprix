'use client'; import {LockKeyhole, Eye, EyeOff, Plus, KeyRound} from 'lucide-react'; import {useState} from 'react';
export default function Loader(){const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [ok,setOk]=useState(false);const submit=async()=>{const r=await fetch('/api/loader',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});setOk(r.ok)};if(ok)return <main className="min-h-screen px-5 pb-20 pt-32"><div className="mx-auto max-w-6xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-primary">Private console</p><h1 className="mt-3 text-5xl font-black tracking-[-.05em]">Loader admin.</h1><div className="mt-10 grid gap-4 md:grid-cols-3"><Panel icon={<Plus/>} title="Add script" text="Publish a new Lua release with metadata and source."/><Panel icon={<KeyRound/>} title="Manage keys" text="Create and revoke trial, monthly, and lifetime access."/><Panel icon={<LockKeyhole/>} title="Private source" text="Source stays server-side and is never shown publicly."/></div><div className="glass mt-5 rounded-3xl p-6"><h2 className="text-xl font-semibold">Script workspace</h2><p className="mt-2 text-sm leading-6 text-muted">Connect the API routes to add scripts, manage visibility, and review activity. This console is intentionally unlinked from public navigation.</p></div></div></main>;return <main className="grid min-h-screen place-items-center px-5"><div className="glass w-full max-w-md rounded-3xl p-7 text-center"><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-primary/15 text-primary"><LockKeyhole/></div><h1 className="mt-6 text-3xl font-bold">Private loader</h1><p className="mt-2 text-sm leading-6 text-muted">Enter the admin password to access your script workspace.</p><div className="mt-7 flex items-center gap-2 rounded-xl border border-white/10 bg-black/20 px-4 py-3"><input autoFocus type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&submit()} placeholder="Loader password" className="w-full bg-transparent outline-none"/><button onClick={()=>setShow(!show)} aria-label="Toggle password visibility" className="text-muted">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><button onClick={submit} className="mt-4 w-full rounded-xl bg-primary py-3 font-semibold transition hover:bg-primary/80">Enter workspace</button></div></main>}
function Panel({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <div className="glass rounded-3xl p-6"><div className="mb-8 text-primary">{icon}</div><h2 className="text-xl font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{text}</p></div>}
